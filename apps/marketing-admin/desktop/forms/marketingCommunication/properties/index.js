const fields = require('./fields');
const layout = require('./layout');
const submit = require('./submit');
const values = require('./values');
const editFields = require('./editFields');
const editLayout = require('./editLayout');

module.exports = {
    fields,
    layout,
    submit,
    values,
    editFields,
    editLayout,
};
