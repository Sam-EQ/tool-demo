const project = require('./project');
const additionalFiles = require('./additionalFiles');
const embedLink = require('./embedLink');
const marketingCommunication = require('./marketingCommunication');

module.exports = {
    project,
    additionalFiles,
    embedLink,
    marketingCommunication,
};
