const Apps = require('./apps');
const CustomRoles = require('./custom_roles');
const Facets = require('./facets');
const Models = require('./models');
const Settings = require('./settings');

const Project = {
    Apps,
    CustomRoles,
    Facets,
    Models,
    Settings,
};

module.exports = Project;
